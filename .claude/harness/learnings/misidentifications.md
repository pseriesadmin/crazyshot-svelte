# misidentifications.md — 오인 카탈로그
# 자동 기록 (HOOK-7) | 세션 재시작 시 최근 5건 자동 로드
# 형식: | 날짜 | 도메인 | 오인 내용 | 올바른 해석 | 원인 | 횟수 |

| 날짜 | 도메인 | 오인 내용 | 올바른 해석 | 원인 | 횟수 |
|------|--------|-----------|-------------|------|------|
| 2026-06-27 | auth/supabase | `createClient` 싱글톤으로 "통일"하면 SSR·CSR 모두 작동할 것이라 판단 | 브라우저는 `createBrowserClient`로 서버 `safeGetSession` 쿠키 공유 필수. `createClient`는 세션 없이 anonymous로 동작 → 채팅 401 | Cursor 세션 독립 운영 중 HOOK-5·frozen file 미적용. 설계 맥락(쿠키 기반 세션 계약)이 Cursor에 전달되지 않음 | 1 |
| 2026-08-31 | harness/TASK.md | 병렬 세션(세션 B)이 TASK.md에서 다른 세션(세션 A)이 이미 작성한 "## NOW — 🔴 CRITICAL: 예약코드 주문단위 통일" 블록을 발견하고, 자신의 컨텍스트 압축(compaction) 이전 기록으로 오인 — "이 세션(현재 대화 스레드)이 직접 확인·수행한 것"이라는 문구로 그 아래에 자기 작업을 이어붙임. 각 세션의 sp3-qa-agent도 상대방 산출물을 "삭제해야 할 중복"으로 지목하는 대칭적 결론을 냄 | 두 세션이 동일 아젠다(Migration 400, reservation_code 주문단위 통일)를 서로의 존재를 모른 채 동시에 수행 중이었음. 세션 B는 Migration 400을 Stage에 재적용(멱등이라 무해)하고 tossPaymentGroupRpc.test.ts 신규블록(5케이스)+reservationCodeResolveOrderWide.test.ts(3케이스)를 독자 작성 — 세션 A의 reservationCodeOrderWide.test.ts(5케이스)와 3중 중복 검증 발생. sp3-qa-agent GATE E 검수 중 발견 | 동일 저장소를 공유하는 두 Claude Code 세션이 동시에 같은 CRITICAL 아젠다를 수행 — TASK.md 헤더/제목이 우연히 동일해 "이 세션(현재 대화 스레드)"라는 자기지시 표현이 어느 세션이 읽어도 자기 얘기처럼 보이는 함정 발생. core-rules.md GP-1(병렬세션 git 충돌 경고)의 TASK.md 버전 | 1 |
| 2026-08-31 | harness/TASK.md (후속·해결) | 위 항목의 해결 방식 — Stephen이 "현재 세션이 최신 수정 세션이니 주 메인으로 반영할 것"으로 세션 A를 메인으로 확정 지시 | 세션 A(reservationCodeOrderWide.test.ts)를 canonical로 삼고, 세션 B가 작성한 두 파일(tossPaymentGroupRpc.test.ts의 Migration 400 블록, reservationCodeResolveOrderWide.test.ts)의 고유 엣지케이스(id순서 뒤섞기·UNIQUE 제약 명시확인·단일예약 code조회)만 세션 A 파일로 흡수 통합 후 원본 중복분 제거(reservationCodeResolveOrderWide.test.ts 파일 삭제, tossPaymentGroupRpc.test.ts는 해당 블록만 제거하고 나머지 Migration 378~399 커버리지는 무손상 유지) — 정리 후 8/8 + 21/21 GREEN 재확인 | 병렬세션 산출물 정리는 파일 단위 승자독식이 아니라 "각 파일의 고유 커버리지를 먼저 식별 → 메인 파일로 흡수 → 완전 중복분만 제거"가 손실 없는 해법임을 확인. 향후 유사 충돌 시 이 패턴 재사용 | 1 |
| 2026-09-01 | promotion/points, membership_grade | 포인트 자동적립(rental_complete) RPC 구현 중 `point_earn_rules.grade_multipliers` 시드값이 `{"BASIC","PRO","CRAZY"}` 키를 쓰는 것을 보고 "실제 `membership_grade_enum`(`none/easy/pop/crazy/admin`) 값과 안 맞는 버그"로 판단 → 임의로 `{"easy","pop","crazy"}`로 "정정"하는 마이그레이션을 작성함(Migration 407 초안) | `easy/pop/crazy`는 고객 등급이 아니라 **정기구독 상품 티어**(database.ts:387 EASY/POP/CRAZY)다. 포인트 배수를 매길 "고객 등급" 체계 자체가 이 프로젝트에 아직 정의돼 있지 않음 — 현재 고객 분류는 인증 상태(일반/학생/구독) 뿐이며 이는 §16 탈퇴 정책과도 무관한 완전히 별개 개념이다. Stephen 지적으로 발견 즉시 grade_multipliers 시드 원복 + RPC의 등급배수 조회 로직 자체를 제거(배수 없이 rate만 적용)로 수정 | `membership_grade`라는 컬럼명과 `MembershipGradeEnum` 타입명이 "고객 등급 체계"를 암시했으나, 실제로는 "현재 구독 중인 상품 티어"를 저장하는 컬럼이었음 — 컬럼/타입 이름만 보고 도메인 의미를 추정, DB 컬럼 코멘트나 Stephen 확인 없이 임의로 "버그"라 판단하고 고쳐버림. 이후 동일 세션에서 Stephen이 명시적으로 "고객 레벨(등급)은 아직 미정: 인증 상태(일반/학생/구독)로만 분류"라 정정 | 1 |
| 2026-09-01 | membership_grade 대소문자 | `/cms/customers` 인증분류 필터 RPC(Migration #409) 작성 시 `src/lib/types/database.ts`의 `MembershipGradeEnum = 'none'|'easy'|'pop'|'crazy'|'admin'`(소문자) 타입 정의를 그대로 신뢰해 `up.membership_grade IS DISTINCT FROM 'none'`(소문자)로 비교 로직을 작성. 이후 `src/routes/cms/customers/+page.svelte`가 디스크에서 `!== 'NONE'`(대문자)로 바뀐 것을 발견하고 "이 비교는 항상 참이 되어 모든 고객이 구독으로 잘못 분류된다"며 **버그처럼 보인다고 사용자에게 지적함** | 실제 DB 제약(Migration #98 CHECK)은 대문자 `'NONE'\|'EASY'\|'POP'\|'CRAZY'`이고 실측 984/984행 전부 대문자였다 — `database.ts`의 타입 정의 자체가 실제 DB와 어긋난 stale 문서였음. 디스크에서 대문자로 바뀐 변경은 버그가 아니라 **Stephen이 이미 올바르게 고친 정정**이었고(Migration #410 `get_customer_list_classification_case_fix.sql`), 소문자였던 원본(Migration #409, 이 세션이 작성)이 오히려 진짜 버그(구독 분기 항상 TRUE, 일반 분기 항상 FALSE)였음 | TypeScript 타입 정의 파일(특히 `database.ts`처럼 수동 유지되는 파일)을 실제 DB 제약·실데이터보다 우선 신뢰함 — "코드가 이렇게 생겼으니 반대쪽이 버그"라는 성급한 판단을 두 번 연속(원 설계 때 한 번, 재검토 때 또 한 번) 반복함. **교훈**: enum/제약 관련 비교 로직을 작성·검토할 때는 타입 파일이 아니라 반드시 실제 DB(`information_schema`/CHECK 제약/표본 SELECT)를 직접 조회해 대소문자·정확한 값 목록을 확인할 것 — 이번처럼 우연히 결과가 맞아도(예: 구독=항상TRUE가 "그럴듯해 보임") 근거가 틀렸다면 다른 케이스에서 반드시 재발함 | 1 |
| 2026-09-01 | RPC 권한/anon lockdown 이력 | production에서 발견한 `promote_draft_reservation`·`set_reservation_options`의 anon EXECUTE 권한을 회수하는 Migration #413을 작성하며, 그 근거로 "두 함수 모두 Migration #262(`global_anon_rpc_lockdown`)의 명시적 잠금 대상 목록에 이미 있던 함수"라고 서술함 | #262 원본 파일을 실제로 열어 확인하지 않고 "이 프로젝트는 anon lockdown 이력이 많다"는 일반적 패턴 기억에 기대 서술 — 실제로는 정반대로, #262의 allowlist(카테고리 ③ "비로그인 게스트 세션이 호출하는 예약 생성 화면")에 이 두 함수가 **잠금 예외(anon 접근 의도적 허용)**로 명시 등재돼 있었다. 즉 2026-08-15 시점엔 anon 접근이 설계 의도였고, 이후 2026-08-19 Migration #306(회원전용 예약 정책)이 `create_hold_reservation`/`create_draft_reservation`에만 `is_anonymous` 차단을 추가했을 뿐 이 두 함수는 그 패치범위에서 누락된 것이었음. REVOKE 결론 자체(현재 #306 정책 기준)는 옳았으나 근거 서술이 틀림 — sp3-qa-agent 검수로 발견 | "이 프로젝트는 anon 권한을 반복적으로 잠가온 이력이 있다"는 상위 패턴 인지가 맞다고 해서, 특정 과거 마이그레이션 파일의 정확한 내용(잠금 대상 vs 잠금 예외)까지 자동으로 맞다고 가정함 — 결론이 옳다는 확신이 근거 검증을 생략하게 만든 케이스. **교훈**: 과거 마이그레이션을 근거로 인용할 때는 "이런 패턴이 있었을 것"이라는 기억이 아니라 반드시 해당 파일을 직접 Read해서 실제 내용(allowlist인지 blocklist인지, 어느 카테고리인지)을 확인할 것 — 결론이 우연히 맞아도 근거 서술이 틀리면 다음에 그 서술만 보고 반대 결론을 낼 위험이 있음 | 1 |
| 2026-09-02 | 테스트/DB, public_holidays | `deliveryCutoffHolidays.test.ts` 3건이 `duplicate key value violates unique constraint "public_holidays_date_country_key"`로 실패하는 것을 보고 sp3-qa-agent와 이 세션 둘 다 "stage DB에 남은 오래된/정리 안 된 테스트 픽스처 데이터가 원인"이라고 진단해 Stephen에게 그렇게 보고함(별도 정리 태스크로 스폰까지 함) | 실제로 충돌한 행은 테스트 잔재가 아니라 **진짜 법정공휴일**이었음 — 2027-05-01(노동절)·2027-05-03(대체공휴일(노동절))·2027-05-05(어린이날), 전부 2026-08-24에 `sync_national_holidays`로 정상 동기화된 실데이터. 원인은 테스트가 `nonSundayDaysFromNow(241~248, 500)`처럼 실제 공휴일 동기화 커버리지(오늘부터 최대 약 490일 이내) **안에 있는** 오프셋으로 날짜를 골라 `insertHoliday(..., 'national')`을 시도했기 때문 — 테스트 코드 자체의 날짜 선택 전략 결함이었고 stage DB 픽스처 잔재도, `delivery_cutoff_settings` CMS 토글 로직도 전혀 문제 없었음. 같은 파일의 `sync_national_holidays` describe 블록은 이미 이 위험을 인지해 `syncTestRangeArgs()`로 오늘+1000~1100일 범위로 격리해뒀는데, `loadCourierClosedDates` describe 블록만 그 격리를 놓치고 있었음. 오프셋을 오늘+1200일 이후(다른 블록의 1000~1100 범위와도 안 겹침)로 이동해 해결 — 실제 공휴일 데이터·CMS 설정 어느 쪽도 건드리지 않음 | "duplicate key violates unique constraint" 에러를 보자마자 "테스트가 자기 자신이 만든 데이터를 못 지웠다"는 가장 흔한 설명으로 성급히 귀결 — 실제 DB를 직접 조회해 그 행이 무엇인지(테스트 픽스처인지 실데이터인지) 확인하지 않고 진단부터 내림. **교훈**: UNIQUE 제약 위반을 "테스트 정리 실패"로 단정하기 전에, 반드시 충돌한 실제 행을 SELECT로 직접 확인할 것 — 이번처럼 그 행이 실은 완전히 다른 정당한 시스템(법정공휴일 동기화)이 만든 진짜 데이터일 수 있고, 그 경우 "정리"는 문자 그대로 실서비스 데이터를 삭제하는 것이 돼버려 훨씬 위험했다 | 1 |
