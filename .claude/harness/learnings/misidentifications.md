# misidentifications.md — 오인 카탈로그
# 자동 기록 (HOOK-7) | 세션 재시작 시 최근 5건 자동 로드
# 형식: | 날짜 | 도메인 | 오인 내용 | 올바른 해석 | 원인 | 횟수 |

| 날짜 | 도메인 | 오인 내용 | 올바른 해석 | 원인 | 횟수 |
|------|--------|-----------|-------------|------|------|
| 2026-06-27 | auth/supabase | `createClient` 싱글톤으로 "통일"하면 SSR·CSR 모두 작동할 것이라 판단 | 브라우저는 `createBrowserClient`로 서버 `safeGetSession` 쿠키 공유 필수. `createClient`는 세션 없이 anonymous로 동작 → 채팅 401 | Cursor 세션 독립 운영 중 HOOK-5·frozen file 미적용. 설계 맥락(쿠키 기반 세션 계약)이 Cursor에 전달되지 않음 | 1 |
| 2026-08-31 | harness/TASK.md | 병렬 세션(세션 B)이 TASK.md에서 다른 세션(세션 A)이 이미 작성한 "## NOW — 🔴 CRITICAL: 예약코드 주문단위 통일" 블록을 발견하고, 자신의 컨텍스트 압축(compaction) 이전 기록으로 오인 — "이 세션(현재 대화 스레드)이 직접 확인·수행한 것"이라는 문구로 그 아래에 자기 작업을 이어붙임. 각 세션의 sp3-qa-agent도 상대방 산출물을 "삭제해야 할 중복"으로 지목하는 대칭적 결론을 냄 | 두 세션이 동일 아젠다(Migration 400, reservation_code 주문단위 통일)를 서로의 존재를 모른 채 동시에 수행 중이었음. 세션 B는 Migration 400을 Stage에 재적용(멱등이라 무해)하고 tossPaymentGroupRpc.test.ts 신규블록(5케이스)+reservationCodeResolveOrderWide.test.ts(3케이스)를 독자 작성 — 세션 A의 reservationCodeOrderWide.test.ts(5케이스)와 3중 중복 검증 발생. sp3-qa-agent GATE E 검수 중 발견 | 동일 저장소를 공유하는 두 Claude Code 세션이 동시에 같은 CRITICAL 아젠다를 수행 — TASK.md 헤더/제목이 우연히 동일해 "이 세션(현재 대화 스레드)"라는 자기지시 표현이 어느 세션이 읽어도 자기 얘기처럼 보이는 함정 발생. core-rules.md GP-1(병렬세션 git 충돌 경고)의 TASK.md 버전 | 1 |
| 2026-08-31 | harness/TASK.md (후속·해결) | 위 항목의 해결 방식 — Stephen이 "현재 세션이 최신 수정 세션이니 주 메인으로 반영할 것"으로 세션 A를 메인으로 확정 지시 | 세션 A(reservationCodeOrderWide.test.ts)를 canonical로 삼고, 세션 B가 작성한 두 파일(tossPaymentGroupRpc.test.ts의 Migration 400 블록, reservationCodeResolveOrderWide.test.ts)의 고유 엣지케이스(id순서 뒤섞기·UNIQUE 제약 명시확인·단일예약 code조회)만 세션 A 파일로 흡수 통합 후 원본 중복분 제거(reservationCodeResolveOrderWide.test.ts 파일 삭제, tossPaymentGroupRpc.test.ts는 해당 블록만 제거하고 나머지 Migration 378~399 커버리지는 무손상 유지) — 정리 후 8/8 + 21/21 GREEN 재확인 | 병렬세션 산출물 정리는 파일 단위 승자독식이 아니라 "각 파일의 고유 커버리지를 먼저 식별 → 메인 파일로 흡수 → 완전 중복분만 제거"가 손실 없는 해법임을 확인. 향후 유사 충돌 시 이 패턴 재사용 | 1 |
| 2026-09-01 | promotion/points, membership_grade | 포인트 자동적립(rental_complete) RPC 구현 중 `point_earn_rules.grade_multipliers` 시드값이 `{"BASIC","PRO","CRAZY"}` 키를 쓰는 것을 보고 "실제 `membership_grade_enum`(`none/easy/pop/crazy/admin`) 값과 안 맞는 버그"로 판단 → 임의로 `{"easy","pop","crazy"}`로 "정정"하는 마이그레이션을 작성함(Migration 407 초안) | `easy/pop/crazy`는 고객 등급이 아니라 **정기구독 상품 티어**(database.ts:387 EASY/POP/CRAZY)다. 포인트 배수를 매길 "고객 등급" 체계 자체가 이 프로젝트에 아직 정의돼 있지 않음 — 현재 고객 분류는 인증 상태(일반/학생/구독) 뿐이며 이는 §16 탈퇴 정책과도 무관한 완전히 별개 개념이다. Stephen 지적으로 발견 즉시 grade_multipliers 시드 원복 + RPC의 등급배수 조회 로직 자체를 제거(배수 없이 rate만 적용)로 수정 | `membership_grade`라는 컬럼명과 `MembershipGradeEnum` 타입명이 "고객 등급 체계"를 암시했으나, 실제로는 "현재 구독 중인 상품 티어"를 저장하는 컬럼이었음 — 컬럼/타입 이름만 보고 도메인 의미를 추정, DB 컬럼 코멘트나 Stephen 확인 없이 임의로 "버그"라 판단하고 고쳐버림. 이후 동일 세션에서 Stephen이 명시적으로 "고객 레벨(등급)은 아직 미정: 인증 상태(일반/학생/구독)로만 분류"라 정정 | 1 |
